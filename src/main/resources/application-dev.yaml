server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/employeedb
    username: prashansrivastava
    password:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true


app:
  welcome:
    name: Prashan

payment-gateway:
  type: GIGIG
  retry-count: 3
  timeout: 5000